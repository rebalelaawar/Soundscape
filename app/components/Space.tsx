import { useEffect, useState } from "react";

const Space = ({ code } : { code: string; }) => {
    
    const [ token, setToken ] = useState<string>( );
    
    const getToken = async ( ) => {
        
    };

    useEffect(( ) => { }, [ ]);
    return <div>
        this is the app
    </div>;
};

export default Space;